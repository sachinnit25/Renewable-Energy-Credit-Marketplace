{
  "name": "rec-marketplace",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "start": "vite",
    "serve": "vite",
    "test": "node --test test/*.test.js",
    "test:contracts": "cd contracts/soroban_rec && cargo test",
    "test:all": "npm test && npm run test:contracts",
    "deploy:contract": "node scripts/deploy_soroban.mjs",
    "interact:contract": "node scripts/interact_contract.mjs",
    "capture-screenshots": "node scripts/capture-screenshots.mjs"
  },
  "dependencies": {
    "@stellar/freighter-api": "^6.0.0",
    "@stellar/stellar-sdk": "^16.2.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "playwright": "^1.62.0",
    "vite": "^6.0.0"
  }
}
